# 页面标题
VUE_APP_TITLE = 管理系统

# 生产环境配置
ENV = 'staging'

# 管理系统/生产环境
# 修改为 真实 IP
VUE_APP_BASE_API = 'https://ip:port/wms-api'
# 修改为 真实 IP
VUE_APP_IMG_URL = 'https://ip:port/wms-api'

# # 修改为 真实 IP
# VUE_APP_BASE_API = 'https://domain.name/wms-api'
# # 修改为 真实 IP
# VUE_APP_IMG_URL = 'https://domain.name/wms-api'
